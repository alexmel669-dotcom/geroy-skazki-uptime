name: Uptime Monitor
on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://geroy-skazki.ru/api/health
          echo ""
      - name: Ping landing
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://geroy-skazki.ru
          echo ""
